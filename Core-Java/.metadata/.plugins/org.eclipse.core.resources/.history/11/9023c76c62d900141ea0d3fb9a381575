package calculator;

import java.util.HashMap;
import java.util.Map;

import stackDLL.stackDLL;

public class Calculator {
    private String expression;

    private Map<Character, Integer> operators;

    public Calculator() {
        this.operators = new HashMap<Character, Integer>();

        operators.put('*', 2);
        operators.put('/', 2);
        operators.put('+', 1);
        operators.put('-', 1);
    }

    public float calc() {
        return 0.0f;
    }

    private String convertToRPN(String expression) {
        StringBuilder rpn = new StringBuilder();
        stackDLL operators = new stackDLL();
        char[] exprArray = expression.toCharArray();
        
        for (int i = 0; i < exprArray.length; i++) {
            Character token = exprArray[i];
            switch (key) {
                case value:
                    
                    break;

                default:
                    break;
            }
        }

        return null;
    }

    public String getExpression() {
        return this.expression;
    }

    public void setExpression(String expression) {
        this.expression = expression;
    }
}
