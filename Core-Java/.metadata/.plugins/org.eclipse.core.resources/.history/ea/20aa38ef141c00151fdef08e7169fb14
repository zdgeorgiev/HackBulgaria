import java.util.ArrayList;

public class CountSort {
    private static int[] numbers = new int[1000];

    private static int[] fillHistogram(ArrayList<Integer> arr, int totalSize) {
        for (int i = 0; i < arr.size(); i++) {
            numbers[arr.get(i)]++;
        }

        return numbers;
    }
}