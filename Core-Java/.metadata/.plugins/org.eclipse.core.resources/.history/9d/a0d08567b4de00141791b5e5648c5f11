package statistics;

import java.util.Collection;
import java.util.HashSet;

public interface Statistic {
    float getMean(Collection<Integer> col);

    int getMedian(Collection<Integer> col);

    HashSet<Integer> getMode(Collection<Integer> col);

    int getRange(Collection<Integer> col);
}