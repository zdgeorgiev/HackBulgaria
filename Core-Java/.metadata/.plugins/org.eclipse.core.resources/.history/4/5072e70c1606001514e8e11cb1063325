package palindromes;

public class Program {

    public static void main(String[] args) {
        String word = "akawwaka";
        printAllPalindromes(word, 0);

        System.out.println(isPalindrome(word));
    }

    private static void printAllPalindromes(String word, int n) {
        if (n == word.length()) {
            return;
        }

        StringBuilder rotation = new StringBuilder();
        rotation.append(word.substring(n));
        rotation.append(word.substring(0, n));
        
        if(isPalindrome(rotation.toString())) {
            System.out.println(rotation);
        }
        
        printAllPalindromes(rotation.toString(), n++);
    }

    private static boolean isPalindrome(String word) {
        for (int i = 0; i < word.length() / 2; i++) {
            if (word.charAt(i) != word.charAt(word.length() - i - 1)) {
                return false;
            }
        }

        return true;
    }
}