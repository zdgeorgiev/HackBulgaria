import java.util.ArrayList;

public class CountSort {
    private static int[] numbers = new int[366];

    public static int[] sort(ArrayList<Integer> arr) {
        fillHistogram(arr);

        return numbers;
    }

    private static void fillHistogram(ArrayList<Integer> arr) {
        for (int i = 0; i < arr.size(); i++) {
            numbers[arr.get(i)]++;
        }
    }
}
