import java.util.ArrayList;

public class CountSort {
    private static int[] numbers = new int[1_000];

    private static void getHistogram(ArrayList<Integer> c) {
        for (int i = 0; i < c.size(); i++) {
            numbers[c.get(i)]++;
        }
    }
}
