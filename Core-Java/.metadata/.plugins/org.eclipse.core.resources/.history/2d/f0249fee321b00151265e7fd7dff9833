public class PriorityQueue {

    private Heap heap;

    public PriorityQueue() {
        this.heap = new Heap();
    }

    public Packet pop() {
        return this.heap.pop();
    }

    public void push(Packet p) {
        this.heap.push(p);
    }
}
