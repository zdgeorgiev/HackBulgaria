package sorting;

import java.util.ArrayList;

public class QuickSort {

    public static void sort(ArrayList<Integer> arr) {
        quickSort(0, arr.size(), arr);
    }

    private static void quickSort(int start, int end, ArrayList<Integer> arr) {

        if (end - start < 2) {
            return;
        }

        quickSort(start, i - 1, arr);
        quickSort(j + 1, end, arr);
    }

    private static int partition(int start, int end, ArrayList<Integer> arr) {

        int pIndex = start;

        return pIndex;
    }
}