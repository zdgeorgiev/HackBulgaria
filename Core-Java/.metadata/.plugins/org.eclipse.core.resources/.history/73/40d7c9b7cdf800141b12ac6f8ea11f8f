package blockQueue;

public class MyRunnable implements Runnable {
    public String name;
    BlockQueue<T> bq = new BlockQueue<T>();

    public MyRunnable(String name, BlockQueue<T> bq) {
        this.name = name;
        this.bq = bq;
    }

    @Override
    public void run() {
        System.out.println("Thread " + this.name + " - " + this.bq.poll());
    }
}
