package matrix;

public class Threshold implements MatrixOperation {
    private final float max = 0.5f;

    public int withPixel(int x, int y, int[][] matrix) {
        float currentPixel = matrix[x][y] / 3;

        if (currentPixel < max) {
            
        }
        
        return 0;
    }
}